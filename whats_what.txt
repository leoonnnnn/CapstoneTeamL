0 (optional). extract_sp_backbone.py takes in a pdb files (just download one from PDB or EMDB) and spits out the sugar phosphate backbone as a pdb file

1 (required). sp_backbone_to_nodes.py can run using extract_sp_backone.py's output OR as a standalone script. inputs: EITHER a pdb file (download from PDB or EMDB). outputs: phosphate nodes (phosphorus atoms) separately, sugar nodes separately

2 (required*). pdb2xyz.py converts pdb files to xyz files


*if you want it as an xyz file
